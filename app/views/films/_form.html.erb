<%= simple_form_for @film do |f| %>
  <%= f.input :title, label: "Título" %>
  <%= f.input :rating, label: "Rating" %>
  <%= f.association :genre, label: "Género"  %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Guardar", class: "btn btn-primary"%>
<% end %>